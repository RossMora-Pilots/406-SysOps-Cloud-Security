# Large binary artifacts tracked via Git LFS (when LFS server is available)
artifacts/** filter=lfs diff=lfs merge=lfs -text
images/** filter=lfs diff=lfs merge=lfs -text
*.pdf filter=lfs diff=lfs merge=lfs -text

# Screenshots stored as regular git objects (NAS remote lacks LFS support)
*.png binary
*.jpg binary
CC/**/screenshots/** binary
